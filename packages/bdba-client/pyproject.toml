[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "bdba-client"
version = "0.15.0"
requires-python = ">=3.11"
dependencies = [
    "cachecontrol",
    "dacite",
    "python-dateutil",
    "requests",
]
